'use strict';

describe('myApp.Perfil module', function() {

  beforeEach(module('myApp.Perfil'));

  describe('Perfil controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var perfilCtrl = $controller('ControladorPerfil');
      expect(perfilCtrl).toBeDefined();
    }));

  });
});