'use strict';

describe('myApp.Registrar module', function() {

  beforeEach(module('myApp.Registrar'));

  describe('Registrar controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var RegistrarCtrl = $controller('ControladorRegistrar');
      expect(RegistrarCtrl).toBeDefined();
    }));

  });
});