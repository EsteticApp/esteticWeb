'use strict';

describe('myApp.SolicitarNuevoServicio module', function() {

  beforeEach(module('myApp.SolicitarNuevoServicio'));

  describe('SolicitarNuevoServicio controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var SolicitarNuevoServicioCtrl = $controller('ControladorSolicitarNuevoServicio');
      expect(SolicitarNuevoServicioCtrl).toBeDefined();
    }));

  });
});
